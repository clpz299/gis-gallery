spring:
  application:
    name: gis-gallery
  datasource:
    url: jdbc:postgresql://172.16.23.128:15542/gis_gallery
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
